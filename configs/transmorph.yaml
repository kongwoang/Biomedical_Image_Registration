seed: 7
data:
  root: data/synthetic
output_dir: outputs/transmorph
model:
  in_channels: 2
  embed_dim: 48
  patch_size: 8
  depth: 2
  num_heads: 4
  decoder_channels: 32
training:
  epochs: 2
  batch_size: 4
  learning_rate: 0.001
  image_loss: mse
  smooth_weight: 0.05
  num_workers: 0

