seed: 222
device: cpu
data:
  root: data/oasis/freesurfer_3d_smoke
output_dir: outputs/freesurfer_smoke/voxelmorph_train
model:
  dim: 3
  in_channels: 2
  base_channels: 4
training:
  epochs: 1
  batch_size: 1
  learning_rate: 0.001
  image_loss: mse
  smooth_weight: 0.05
  num_workers: 0
